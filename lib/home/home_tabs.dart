import 'package:flutter/material.dart';

class HomeTabs extends StatelessWidget {
  final _controller;

  HomeTabs(this._controller);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
